controller_credentials:
  - name: "EDA Workshop AAP Credential"
    desc: "AAP credential for EDA workshop (avoids clash with Default org)"
    org: "eda_opentelemetry_workshop"
    type: "Red Hat Ansible Automation Platform"
    inputs:
      host: "{{ aap_hostname }}"
      username: "{{ aap_username }}"
      password: "{{ aap_password }}"

  - name: "EDA Workshop EDA Credential"
    desc: "EDA credential for workshop (avoids clash with Default org)"
    org: "eda_opentelemetry_workshop"
    type: "Red Hat Event-Driven Ansible"
    inputs:
      eda_host: "{{ aap_hostname }}"
      eda_user: "{{ aap_username }}"
      eda_pass: "{{ aap_password }}"

  - name: "ITSM Credential"
    desc: "ITSM Credential"
    org: "eda_opentelemetry_workshop"
    type: "ITSM"
    inputs:
      itsm_host: "{{ itsm_host }}"
      itsm_username: "{{ itsm_username }}"
      itsm_password: "{{ itsm_password }}"

  - name: "ITSM Incidents Credential"
    desc: "ITSM Incidents Credential"
    org: "eda_opentelemetry_workshop"
    type: "ITSM Incidents"
    inputs:
      itsm_incident: "none"

  - name: "OpenShift Demo Credential"
    desc: "OpenShift API credential for demo remediation"
    org: "eda_opentelemetry_workshop"
    type: "OpenShift Demo"
    inputs:
      openshift_api_host: "{{ openshift_api_host }}"
      openshift_api_token: "{{ openshift_api_token }}"
      openshift_namespace: "{{ openshift_namespace }}"
